<div class="post archive">
  <table class="archive">
    <thead>
      <th>All Posts</th>
      <th></th>
      <th style="text-align: right;">Reads</th>
    </thead>
  <tbody>
  {{#each posts}}
    <tr>
    {{#if published}}
    <td><a href="/posts/{{ date }}/{{ file }}.html">{{ title }}</a></td><td style="color: #ababab">{{ display_date }}</td><td style="color: #ababab">{{ pageViews }}</td>
    {{/if}}
    </tr>
  {{/each}}
  </tbody>
  </table>

</div>
